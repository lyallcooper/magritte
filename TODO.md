- [x] We're currently using what appears to be the git status --short output. E.g. "??", " M", "MM", etc. That's not very user friendly, so let's replace it with something more obvious. Feel free to look at magit for inspiration.
- [x] Settings screen where we can set the font and theme. Should take effect immediately without restart or save button so previewing is easy.
- [x] A special debug mode that lets us send emulate keyboard commands directly without having to use apple script, and also lets us screenshot the window without having to use system APIs. This should speed up development, and allow us to do it without needing to forground the window.
- [x] cmd+q and cmd+w don't work
- [x] Auto light/dark mode switching support with system theme
- [x] Maybe we should limit shown fonts to only monospace ones?
- [x] Mouse support. Not everything needs to be 100% keyboard driven, eg the settings screen. A dropdown would be appropriate, for instance
- [x] Theme list should be alphabetized
- [x] We don't need to show "enter" for newline on the commit message screen
- [x] X-ing out the last window should close the app altogether
- [x] Cmd+, should also open settings on macOS, since that's the standard shortcut
- [x] The app menu Magritte > Settings should open the settings as expected
- [x] The outline on the count tags should be a little smaller, not quite as tall
- [x] Use "Return" instead of "Enter" for the name of the key
- [x] Should we use GPUI component collapsible for the file diff headers? — DECIDED NO: Collapsible renders its content as regular (non-virtualized) child elements. Our diff is a single flat uniform_list of all rows (sections/files/hunks/lines) so only on-screen rows are realized; Collapsible would eagerly build every line of an expanded diff, breaking the large-repo performance that's a core goal.
- [x] The file count tag slightly touches the first line of the diff when it's expanded, which doesn't look great. Maybe we should use tag (small) instead? And I prefer the default partially rounded look instead of fully rounded, looking at the component gallery (https://longbridge.github.io/gpui-component/gallery/)
- [x] Clicking outside of the ? dispatch area should dismiss it
- [x] The commit and cancel buttons on the commit editor screen should be clickable. In general, whenever we show keys like that they should be clickable buttons that perform the associated action
- [x] Default theme (when unconfigured) should be Default Light/Dark, appearance auto, and font should be system default monospace UI font.
- [x] When in visual mode, the current line should be the same color as the highlighted lines
- [x] kbd + label buttons should show mouseover by highlighting just the text label, not the kbd element too. But clicking on the kbd should work still.
- [x] Right click actions. E.g. click on a file name or hunk or selection to stage, unstage, discard, etc.
- [x] The discard confirmation "Discard unstaged ... (y/n)" bar partially overlaps the '?' button in the bottom right, which looks ugly.
- [x] The discard confirmation y/n should be clickable kbd buttons
- [x] Click and drag should select like 'v'.
- [x] Commit editor should show the diff to be committed like magit does
- [x] Visual mode indicator should use standard kbd button keys like everywhere else
- [x] Tab inside a hunk collapses (or expands) it, just like it magit
- [x] Opening the settings screen prints warnings like "2026-06-23 11:40:38.247 magritte\[99253:4332374] CoreText note: Client requested name ".ZedSans", it will get TimesNewRomanPSMT rather than the intended font. All system UI font access should be through proper APIs such as CTFontCreateUIFontForLanguage() or +\[NSFont systemFontOfSize:]."
- [x] When perfoming an action that changes the status UI, eg staging/unstaging/folding/unfolding, we can improve where we move the cursor line to after the action is performed to make it more user friendly and more like magit. We should bias the cursor line totay within the current section whenever possible. Folding inside a hunk should put us on the header line of that hunk. In general we shouldn't keep the cursor line number the same and hope it makes sense
- [x] Visual selection should allow us to stage whole files that are selected. Eg if the file diff is not expanded, the file name row being selected should make stage/unstage/etc. actions act upon it
- [x] Trying to start a regular commit should prompt to stage files like magit does
- [x] Shift-click on the status page should also start visual mode selection, with the selection going from the previous cursor position to where the shift click targeted
- [x] The commit editor should highlight chars in the commit title line that go past 50 chars, and we should auto-hard wrap the text after 72 chars in the body. Both of these should be toggleable in the settings. I'd also like some way of being able to reflow the text when editing after the auto-wrap, but not sure how to best expose that
- [x] Confirm canceling a commit if there's an in progress commit message, or if the message has been modified in the case of a reword
- [x] Make the count tags a little smaller still
- [x] Implement branch transient and commands — checkout (DWIM tracking for remote branches), create, create & checkout, rename (two-step), delete. Deferred magit extras: spinoff/spinout, reset, orphan, worktree, shelve, configure, and choosing a non-HEAD start point when creating.
- [x] Actually make the count tags not tags at all. Just a number in a subtle shade should do the trick
- [x] Ability to view raw git command log — `$` opens a process-buffer view (magit's `$`); Repo records every invocation in a shared ring buffer.
- [x] Mouse hover subtle highlight on file/diff rows
- [x] '$' under "essential" on the '?' menu doesn't feel right—I wouldn't call it essential — moved to "Commands"
- [x] Sketch out how we might customizable user actions in the future, a la magit and its transient-append-suffix et al functions that let users extend magit to run arbitrary commands. We could scope ours to be a little simpler, since we're not inside of emacs, but it'd be cool to let users remap or unbind existing commands, and to add their own custom commands — sketched in docs/extensibility.md. Foundation built: the `commands()` registry exists and the keymap/`?`-menu/`:`-palette all resolve through it. Still deferred: the `[keymap]` remap/unbind table and `[[command]]` custom commands.
- [x] Command dispatch, in the spirit of M-x, but accessible via ':'. Might need to sketch this one out first too. — done: a command registry (`commands()`) is now the single source of truth; `:` opens the vertico picker over it (`PickerAction::RunCommand`), and the `?` menu is generated from it. (Remap/unbind/custom commands from #42 still deferred.)
- [x] The background in solarized dark theme feels a little dark. Why might that be? — base03 is the canonical (intentionally dark) Solarized bg; the panel now elevates to base02 so the UI isn't uniformly the darkest shade
- [x] Allow users to set a UI font that is used to render non-tabular or code-type text. So something that would be ok for proportional fonts — done: opt-in `ui_font` config + a "UI font" settings dropdown (the editor/monospace font is now its own "Editor font" control). The UI font is the root default for prose chrome (headers, menus, labels, palette); code/diff/tabular rows and the editor/log/git-log/commit views override back to monospace. Empty `ui_font` = monospace everywhere, as before.
- [x] I think I prefer how magit orders the transient window contents. We seem to only allow a basic column layout, but magit's layout is more flexible and makes more efficient use of screen space — done: magit's layout, derived from content (per transient.el's model: full-width argument sections stacked, command groups side by side). An *argument* group (Switch/Option) is a full-width band, bands stack vertically, and a tall band fans into sub-columns (~7 rows each); the *command* groups (Action/`?`-Info) sit side by side in a wrapping row beneath. Reproduces magit's commit transient (Arguments over a Create/Edit row), the log transient (Arguments over the Log row), and the `?` dispatch (all command groups packed in a row) with no per-transient layout spec. `render_group(subcols)` / `render_suffix`.
- [x] '$' and ',' feel like they don't belong in the commands section, since they're not git commands but app-related commands. Maybe we need a new section — done: registry commands carry a `Category`, and Settings/Git command log now render in their own "Application" group in the `?` menu.
- [x] The "Head:" and "Push:" lines at the top shouldn't be rendered the same as the diff lines. Maybe they should be in a DescriptionList? Or something that makes sense. And I like our stylized rendering of the branch that we use elsewhere, let's use that there too. — done: a `RowKind::Header` renders a dim fixed-width label + the value as the stylized branch chip (shared with dialog titles) + optional dim ahead/behind; the header rows are non-selectable chrome now.
- [~] How might we support text selection for copy and paste? It ok if it's mouse only if that's easier — done (tier 1, line/unit copy): `y`/Cmd-C/right-click-Copy yank the visual selection or the line at point in the status view AND the commit-diff view (which gained a cursor + visual selection), and `y` in the log copies the selected commit's hash. Copy buttons on the title-bar branch and the commit hash. All copies flash a "Copied …" confirmation. DEFERRED (tier 2): character-level drag-selection of arbitrary text — GPUI gives the primitives (`TextLayout::index_for_position`, `StyledText::with_highlights`), but it needs a cross-row selection layer over the virtualized list and a different mouse gesture (drag is already line-select). Also not yet: line selection/copy in the `$` git-log view (it scrolls, no cursor).
- [x] And some basic emacs key bindings, such as C-x C-c — done: C-g is a universal cancel (= Esc, incl. the picker/editor), C-n/C-p move down/up (= j/k, incl. minibuffer nav in the picker), and C-x C-c quits (via a `pending_cx` prefix like the `g` prefix).
- [x] Have some type of frecency ranking for e.g. command palette commands. Anywhere else this might be appropriate? — done for the `:` palette: a decaying per-command score (persisted to command-usage.toml) ranks the list, most-used-recently first; fuzzy matching takes over once you type, with frecency breaking ties. Only palette runs are recorded — a command you already invoke by key doesn't need surfacing. (Could later extend to ranking branch/remote pickers by recent use.)
- [~] Finish #42's config-driven customization (the registry foundation, `:` palette, and category grouping are done). (1) `[keymap]` remap/unbind — DONE: a `[keymap]` table (keystroke → command id, `"unbound"` to remove) is overlaid on the registry defaults into an *effective keymap* at startup; `on_key`/`run_dispatch` resolve through it (so a key can bind to *any* id, including leaves like `branch-delete`), which also removed the hardcoded key→id arms in `on_key`. Unknown ids warn at startup. Only top-level command keys are remappable (motions/prefixes stay fixed), per the design. Not yet: the `:` palette key-hint still shows the *default* binding, not the remap. (2) `[[command]]` custom commands — DONE: a `[[command]]` table (`id`/`title`/`run`/`then`/`refresh`) defines a git invocation the `:` palette and `[keymap]` run by id. Rather than reworking the const fn-pointer registry, user commands live in config and `invoke_command` falls through to them: runs `git <run>` then `git <then>` on the background path (logged/cancellable), with `{file}`/`{commit}`/`{branch}` placeholders resolved against the selection and a confirm for clean/--hard/--force. Git-only by design (the `!` prompt covers one-off non-git commands). Still deferred: tightening the permissive `enabled` predicate (always-true today) toward magit's `:if` suffix predicates so the palette hides inapplicable commands.
- [x] Filter out all the status view generated git status and git diff commands out from the command log by default, optionally show them with a — done: `GitCommand::is_query()` marks the UI's read-only queries (status, diff, rev-parse, for-each-ref, show-ref, ls-files, symbolic-ref, config --get); the `$` log hides them by default and `a` toggles "show all" / "hide queries".
- [x] Return on file to open in external $EDITOR — done: Return on a file row (or a hunk/line within it) opens it in a configurable external editor; also in the `?` menu (Essential) and `:` palette, and the config-file button uses the same path. Rather than sniff `$EDITOR` (a GUI process can't honor a terminal editor reliably), there's an explicit `editor` command in Settings: empty = OS default opener; otherwise the GUI editor (e.g. `code -w`, `zed`) is spawned directly. Terminal/TUI editors are intentionally out of scope — there's no prompt-free, terminal-agnostic way to launch one from a GUI app on macOS (iTerm prompts on `.command`, every terminal has a different launch dialect), so we don't try.
- [x] Style title bar like rest of app, include more useful info in it. Like zed does. — done: a custom transparent title bar (gpui-component `TitleBar`, via `render_title_bar`) drawn in the app palette (bg/border match the window), shown above every view (status/settings/editor/log). It shows the repo name, the current branch as a chip, ahead/behind vs upstream (`↑n ↓m`, when diverged), and a dirty dot when there are uncommitted changes. The component handles traffic-light spacing, window dragging, and off-macOS window controls.
- [x] Clicking on the branch in the title bar should bring up the branch transient. Likewise clicking on the ahead arrow/number should bring up push transient, and on the behind arrow/number the pull transient — done: the title-bar branch chip, `↑ahead`, and `↓behind` are clickable (`titlebar_action` → `invoke_command` for "branch"/"push"/"pull"), brightening on hover.
- [x] Auto-refresh the status view (lower priority than M6/M7). — done (option a): a window-activation observer re-runs `git status` when the window regains focus, on the status screen, opt-out via `refresh_on_focus` (default on). Deliberately not a worktree FS watcher (the large-repo event-storm hazard the design avoids). Interval-timer (option b) not added — focus-refresh covers the common case.
- [ ] M7 deferred extras (the fundamentals — merge/rebase/cherry-pick/revert/reset, in-progress sequence surfacing, take-ours/theirs conflict resolution — are done; these are magit's higher-effort/niche additions on top):
  - **Interactive rebase** (`r i`): DONE (v1) — the rebase transient's "Interactively" action prompts for a base, then opens a todo editor over `base..HEAD` (action · hash · subject; j/k move, p/e/s/f/d set pick/edit/squash/fixup/drop, shift+J/K reorder, Return start, Esc cancel). Runs as one `git rebase -i` with the edited todo injected via a throwaway sequence editor; `edit`/conflicts pause into the existing banner. Core `Repo::rebase_todo`/`rebase_interactive` + tests. `--edit-todo` on an already-paused rebase is now DONE too (`r e` — reuses the todo editor on the remaining steps via `Repo::rebase_current_todo`/`rebase_edit_todo`). Reword is DONE too (the todo editor's `r` action plus the rebase transient's `w` reword-a-commit shortcut, pausing into the in-app message editor), as is the autosquash shortcut (`r f`). STILL DEFERRED: inline squash-message editing (`GIT_EDITOR=true` keeps the combined message) and the `magit-rebase-edit-commit`/`remove-commit` shortcuts.
  - **Merge variants**: absorb, dissolve, preview, and "merge and edit message" (we do plain/no-commit/squash with the default message). Octopus merge (multiple targets) — we take one.
  - **Reset modes**: ~~index-only (`git reset <commit> -- .`) and worktree-only (`git checkout-index` via a temp index)~~ — DONE: `O i` (index-only) and `O w` (worktree-only, via a throwaway index → checkout-index; confirms like hard since it overwrites the worktree). `ResetMode::Index`/`Worktree`.
  - ~~**Cherry-pick / revert transient**: switches (`-x`, `--mainline=N` for merge commits, `--edit`) and applying a range/multiple commits.~~ DONE: both are transients with switches (cherry-pick: `--ff`/`-x`/`--edit`/`--signoff`/`--mainline=N`; revert: `--signoff`/`--mainline=N` — `--edit` deliberately omitted, revert always takes git's default message) and an `r` range action.
  - **Conflict resolution depth**: interactive (ediff-style) per-hunk resolution and `--merge` (restore conflict markers). We offer take-ours/take-theirs + stage-to-resolve.
  - **Rebase niceties**: subset rebase (`magit-rebase-subset`) and choosing a non-HEAD start point. ~~`--autostash` defaulting on~~ — DONE: `Switch` gained a `default_on` flag (seeded into the transient's active set on open); rebase `--autostash` now starts on, like magit.
  - ~~**`git am`**: we detect/surface a paused `am` but there's no command to *start* one.~~ DONE: the `W` patch transient's `w` action applies a mailbox as commits (`git am`), pausing into the existing am banner on conflict.
- [x] Open file should open to where the diff starts — done: Return opens the file at the new-side line of the hunk/line at point (or, on a file row, its first hunk). Line targeting works for recognized editors via their goto convention — `+N file` (vim/nvim/emacs/nano/…), `--goto file:line` (VS Code family), `file:line` (Zed/Sublime/Helix) — resolved from a CLI command or, on macOS, an app name → its bundle CLI. Unknown editors / the OS-default opener still open the file (no line).
- [x] Arrow keys should work for nav — done: Up/Down move the selection in the status list and the log view (alongside k/j).
- [x] Why is reset 'X' instead of 'O'? 'O' is the binding I see in magit — done: reset is now `O` (the evil-collection-magit binding); `X` is freed.
- [x] '!' to run command like magit — done: `!` (and evil-collection's `|`) opens a free-text "git:" prompt; the typed args run as `git …` (a leading "git" is stripped), recorded in the `$` log, with the first output line shown and a refresh after. Output beyond the first line isn't surfaced yet (the `$` log records the invocation + stderr); a full process-output view is a future enhancement.
- [x] Other low-hanging magit commands, like 'i' ignore — done: `i` opens a gitignore transient (magit's): `t` shared toplevel `.gitignore`, `s` shared subdir `.gitignore` (the file-at-point's dir), `p` private `.git/info/exclude`, `g` global `core.excludesFile`. Each opens a free-text pattern prompt seeded with the file at point (basename for the subdir case, repo-relative path otherwise); the rule is appended (creating the file/dirs as needed) and the tracked ones (`t`/`s`) are staged, like magit. Core `Repo::add_ignore_rule`/`IgnoreDest` + tests. Skip-worktree / assume-unchanged (magit's `w`/`u` in the same transient) not included.
- [x] Why is 'y' yank not a command in the palette? — done: yank was wired directly in `on_key` and never registered. Added a `yank` ("Copy", `y`, Essential) command to the registry, so it now appears in the `?` dispatch menu and the `:` palette; `on_key` (`y` / Cmd-C) routes through `invoke_command("yank")` like the other commands.
- [x] Surface errors in config file — done: startup already showed parse/read warnings via `load_reporting`; the config-file watcher now does too — an external edit that no longer parses surfaces the TOML error (with line/column) and keeps the live config rather than silently resetting to defaults.
- [x] Updating config externally should be reflected in the open settings screen. Shouldn't have to close and re-open the settings screen. If this is too difficult to do it's ok to leave as-is
- [x] Rebasing (and merging etc) should have the same keybindings as magit. E.g. `r r` should be rebase continue — done: while a rebase is paused, `r` opens a continue/skip/edit/abort transient (`r r` continue, `r s` skip, `r e` edit-todo, `r a` abort); a merge in progress opens its abort (`m a`). The in-progress banner's actions are standard keycap+label buttons showing the full keystroke (`r r`/`m a`), not a bare key that would collide with "open rebase". `r e` reuses the todo editor on the paused rebase's remaining steps via `git rebase --edit-todo`.
- [x] The '!' to run git command should allow arbitrary commands, not just git (though git should be the default). Also we should show the command in the git command log (if it's a git command). And we should not only show the last line of output for the result — done: the `!` prompt opens prefilled with `git ` (magit-style); the first word picks the program — `git` runs a subcommand via the repo wrapper, delete the prefix to run any other program in the worktree (`ls -la`). User commands record full stdout+stderr in the `$` log (internal calls stay lean), shown under their own program name; a multi-line result auto-opens the `$` log. `GitCommand.program/user/stdout` + `Repo::run_user`.
- [x] There shouldn't be any special reserved keys such as Tab, Ctrl-x, Space, etc. We should have a unified keymap for all mappings, including secondary bindings — done: a `chord()` encoder (modifier prefixes `C-`/`M-`/`D-`) + preset binding tables layer all secondary aliases (arrows, C-n/C-p, Space, C-f/C-b/C-d/C-u, section movement aliases, C-x C-c) onto the registry's primary keys, before the user `[keymap]` — so every motion/page/quit binding is remappable and unbindable. `try_nav` resolves all motions through the keymap (hardcoded alias match removed); `pending_cx` gone (C-x is a generic prefix). Only Esc/C-g (keyboard-quit), Tab fold, and modal input stay outside the keymap.
- [x] The banner that we show after a prefix key has been pressed should be different. It should be on the bottom, and it should be lighter weight. It should only show the key that has been pressed. And this goes against the recently implemented key delay feature, but I'd like us to have which-key type popup after a prefix key is pressed (after a delay of maybe 1000ms). What do you think? — done: adopted the which-key model. A prefix now waits indefinitely for the next key (no auto-dismiss / no fire-on-timeout); pressing it shows a lightweight bottom strip with just the pressed key, and after `which_key_delay_ms` (renamed from `prefix_timeout_ms`, default 1000) the strip expands into the list of continuations. The one capability dropped: a key bound as *both* a command and a prefix no longer fires its command on timeout (a prefix is a prefix) — no default binding did this.
- [x] There's no reason we should only allow keybindings two levels deep (i.e. one prefix only). May as well make it generic, right? — done: a pending sequence now accumulates keys to any depth, re-classifying after each key as a complete binding / deeper prefix / unbound. which-key regroups by the next key (deeper sub-prefixes show "…"). `build_keymap` no longer caps depth.
- [x] If the users presses a key (or key sequence) that isn't bound, we should display a toast saying that that key sequence is unbound — done: an unbound key or sequence shows "… is unbound" (fading notice). Single keys toast only without cmd/alt/ctrl so OS/editor shortcuts don't trigger false notices.
- [x] In progress prefix keys should be shown inside a kbd, as should the key in 'x is unbound'. — done: the pending-prefix strip and the "… is unbound" notice render the typed keys as a keycap (one kbd element for the whole sequence, e.g. `g z`), via a `status_keys` field on the keys-led notice.
- [x] If a key sequence ends with Esc (or C-g), it shouldn't trigger the "x Esc is unbound" toast — done: Esc/C-g while a sequence is pending aborts it silently (keyboard-quit, not a binding attempt).
- [x] Can we add custom --flags to transients, like in magit? — done: a `[transient.<id>]` value is a `TransientSuffix` — a bare string (command id, or a `-`-prefixed flag) or a table `{ flag, description }` for a labelled switch. `Switch` now holds owned Strings so injected switches are ordinary `Suffix::Switch` (toggle/args/render unified with built-ins). Validation warns on a non-dash switch key; built-in suffix keys win over injections.
- [x] Magit has a feature that lets users save switch configuration, i.e. change what the default switches are. How might we implement that? — done (magit's `transient-save`): `C-s` inside a transient saves the current argument defaults, persisted to `transient-arguments.toml` (sibling of config, `commit = ["-a","-s"]`). On open, `open_transient` seeds the active set from the saved entry (overriding `default_on`). `TransientState` gained an `id` (and a `baseline` to detect changes); on_key passes the full chord into `handle_transient_key` so `ctrl-s` works; a hint at the *top* of the popup advertises it only once the arguments differ from the baseline — top placement (the popup is bottom-anchored) means it grows upward into empty space, so no layout shift and no reserved dead space.
- [x] Can we let users place their custom bindings in existing transient sections, instead of just lumping them in "Custom"? — done: a `[transient.<id>]` table form takes an optional `group` (section title); `open_transient` appends each injection into the matching section (creating it if absent). Defaults: switches → "Arguments", actions → "Custom". `TransientSuffix` gained an `Action { command, group }` table variant + `group` on `Switch`.
- [x] Do we allow custom keybindings using modifier keys? If not we should — done: they parsed into the keymap but only motions (via `try_nav`, which builds the chord) dispatched; the general status-view dispatch resolved on `cased` (the shift-cased key with modifiers stripped), so a non-motion binding like `[keymap] "ctrl-d" = "commit"` was silently dead. The `_` dispatch arm now resolves on the full `chord(key, shift, ctrl, alt, cmd)` — for a plain/shifted key the chord is just its cased form, so nothing else changes; an unbound modifier chord still suppresses the "… is unbound" toast (it's usually an OS/editor shortcut).
- [x] Add some more of the built in flags that magit has, such as commit's -R/D/A/S etc. — done: the commit transient gained `-R` (`--reset-author` switch), `-A` (`--author=` option, author-name completion), and `-D` (`--date=now` switch — magit's `-D` defaults to now). `-S`/`--gpg-sign` is handled by the config-aware switch work below (it wants enabled-by-config + `--no-gpg-sign`, not magit's key-taking option form).
- [x] We respect git config when running commands, right? If so, we should mark flags as enabled when they are effectively enabled by config. Such as commit --gpg-sign being enabled. Then toggling the flag should send the reverse (eg --no-gpg-sign) — done: we shell out to git, so it already honors config. Added a *negatable* switch — `Switch::negatable(key, arg, negation, config_key, …)` — and used it for commit's `-S` (`--gpg-sign` / `--no-gpg-sign`, keyed to `commit.gpgSign`). `open_transient` seeds such a switch's `default_on` from `repo.config_bool(key)` at open time (synchronous, like `remote_targets()`), so it renders enabled when config enables it; `args()` then emits a flag only when the toggle *differs* from the configured default — the positive arg when on, the negation when off. Unit-tested across the four config×toggle cases.
- [x] Per-repo settings, starting with transient-value defaults — done (first increment): a repo "scope" (`.git/magritte/`, via `git rev-parse --git-common-dir`, worktree-safe) mirrors the global config dir's layout. Saved argument defaults now have two scopes — `Ctrl-s` then `g`(lobal)/`l`(ocal) chooses where to write; on open the repo scope wins over global per transient id, then built-in (incl. git-config-derived) defaults. Both files live-reload. Foundation (discovery + scope dir + watcher) is reusable.
- [x] Per-repo general config (`config.toml` in `.git/magritte/`, deep-merged over the global one) — done: `config::load_merged` reads the global config as a TOML value and overlays the repo scope's `config.toml` (`deep_merge`: scalars/[keymap]/[transient] merge entry-by-entry with the repo winning, `[[command]]` concat + dedup-by-id), then deserializes. `new()` resolves the merge after repo discovery (re-applying appearance so a repo theme paints from frame one); the watcher treats either config file's change as one merged re-resolve. Unit-tested. The Settings screen stays global-only by decision — hand-editing `.git/magritte/config.toml` covers per-repo needs, so no repo/global save-scope toggle in the UI.
- [x] Audit all places we output keybindings and make sure they're in kbd, or an explicit place where we don't want that (eg in the command palette) — done: swept every keystroke display. Keycaps (`key_chip`/`switch_chip`) already cover the pending-prefix strip, the unbound notice, the `?` dispatch menu, transient suffixes, and the save hint. Fixed the one raw-text holdout — the running-job "Ctrl+g to cancel" hint now renders `[Ctrl]+[g]` as a keycap — and ran the toast's "press <key> for the full output" tail through `format_keys`. The deliberate plain-text exception is the `:` command palette's binding hint, where a keycap would be too tall for those rows.
- [x] Status sections like magit — unpushed / unpulled / recent commits / stashes, configurable — done: `SectionId` gained the commit/stash sections; `rebuild_rows` loops over `[status].sections` (an ordered id list, presence = included; default is a magit-like order) dispatching to `push_section` / `push_commit_section` / `push_stash_section`. Lists fetched on the background refresh path (`Repo::unpushed`/`unpulled` reuse `log_with`; recent reuses `log`; `stash_list` existed). Per-repo configurable (rides the config merge). Act-at-point: commit row `Return` opens its diff (Commit screen generalized to return to status via `back: Box<Screen>`), `y` yanks the hash; stash row `Return` shows / `a` apply / `A` pop / `x` drop (confirmed). Unit-tested config defaulting + unknown-id warning; verified live.
- [x] More magit status sections — done: triangular **pushremote** sections (`unpushed-pushremote`/`unpulled-pushremote`, vs `@{push}`; fetched only when a distinct push target exists, interleaved into the default order, hidden otherwise). An opt-in **ignored files** section (`ignored`, off by default; `Repo::ignored_files`). **Tags** two ways, matching magit: ref labels (branches/tags/remotes, colored) on commit rows, and a title-bar `Tag: v1.0 (5)` / `Tags: …` segment (nearest tag behind + ahead via `Repo::tags_around`), toggleable with `show_tags` (default off). The in-progress sequence already renders its steps in the banner, so it was left as-is.
- [x] Status commit-row cherry-pick / revert / rebase-since — done, Magit-style with evil-collection keys: status commit rows now feed commit-at-point into the action layer. `A` opens a cherry-pick transient (`A` pick / `a` apply without committing), `_` opens a revert transient (`_` revert commit / `-` revert without committing), lowercase `a` and `-` run the no-commit variants directly, and `r` opens the rebase transient whose `i` action rebases since the row's commit. The shared `selected_commit_hash()` still lets log-view shortcuts and status commit rows use one target path.
- [ ] Bigger magit features still missing (from the M7/Tier survey) — first slices are done: a **diff transient** (`magit-diff`: dwim, arbitrary refs/ranges, unstaged, staged, worktree, show commit) opens read-only diff buffers with the usual diff rendering/navigation; **tags** can be created/deleted; **remotes** can be added/renamed/removed. Blame, bisect, and patch/`git am` are now done (see below). Bigger remaining areas: **submodules**, fuller **worktree management**, plus deeper remote configuration parity tracked below.
- [x] 'V' should still activate visual line mode — done: in the default evil-collection preset, `V` toggles the (already line-wise) visual selection like `v`, remappable like any default. In the optional vanilla preset, `V` is left for Magit's commit-at-point revert behavior instead.
- [x] Toggle in settings for auto-refresh on focus — done: a new "Behavior" settings section with a `refresh_on_focus` toggle (reusing `toggle_control`).
- [x] Toggle in settings for showing tag in titlebar — done: a `show_tags` toggle in the same "Behavior" section.
- [x] Some way of editing or opening the repo-specific config from the settings screen — done: an "Open repo config" header button (shown when there's a repo) that creates/opens `.git/magritte/config.toml` in the editor.
- [x] Support removing a command/flag from a transient — done: `[transient.<id>]` now honors a keymap-style `"key" = "unbound"` sentinel that drops the built-in suffix at that key (e.g. `"-n" = "unbound"` removes commit's `--no-verify`); pair with a new binding at the same key to replace. `TransientSuffix::is_unbound`; `open_transient` removes matching suffixes (and empties) before injecting; validation skips the sentinel.
- [x] if I stage an untracked file with `git add -N`, it shows up as unstaged still. The empty new file should show in the staged section I think (like magit) — done (I was wrong at first): magit *does* show intent-to-add in **both** — a **new file in Staged** (the empty index placeholder) and a **modification in Unstaged** (the content). It lists staged files via `git diff-index --cached HEAD`, which shows the ita as `A` even though plain `git diff --cached` hides it (default `--ita-invisible-in-index`). Detect ita in the porcelain (`FileEntry::is_intent_to_add`: tracked, index `.`, worktree `A`), count it as staged, and have `section_change` label it "new file" in Staged / "modified" in Unstaged so it appears in both. Tested + verified live against the magit screenshots.
- [x] Custom toplevel commands should show in the help transient. And should also be allowed to be put in specific sections from the config like we do for other custom commands — done: `dispatch_menu` now also lists bound `[[command]]`s; each is placed in its `section` (`[[command]]` field, default "Commands", creating the group if absent), mirroring transient injection. `Info.description` became owned (`String`) so runtime command titles fit.
- [x] A way of remembering which sections are expanded/collapsed across restarts (per repo?) — done: section fold state persists to a per-repo `.git/magritte/folds.toml`. Sections are expanded by default, so only the collapsed ones are stored (by config id); `new()` seeds the default-expanded set then re-collapses what the file lists, and toggling a section fold rewrites it. File/hunk folds stay ephemeral so reopening loads no diffs. DRYed the all-sections list into `SectionId::ALL`.
- [x] Background auto fetch option? Also per-repo — done: a `[fetch]` config (`auto`, `interval_minutes`, off by default) runs a periodic background `git fetch` (the current branch's remote, via new core `Repo::fetch_default`) off the UI thread and refreshes when it lands. A generation-scoped loop restarts cleanly when `[fetch]` changes; it's quiet, skipped while another job runs, and tolerant of failures (offline → retry next tick). Per-repo overridable; documented in docs/config.md.
- [x] We can probably hide some of the common navigation keybindings from the help transient. Ctrl-d/u/f/p, g/GG for example. — done: dropped the whole Navigation group from the `?` menu (it force-showed all motions past the `menu` flag). Cursor motions are standard vim/emacs conventions and magit's dispatch doesn't list them either; they still dispatch and remain remappable. The coverage test lists them as intentional OVERRIDES.
- [x] Codebase review + refactor — done: full read of both crates + docs. Fixed bugs (cancellation/timeout bypassed in the `run_*` family; couldn't stage a manually-resolved conflict; `{commit}` placeholder on status commit rows), moved `parse_refs`/`is_conflicted` off the render hot path, a DRY sweep (shared diff-args/commit-mode/atomic-TOML-write helpers), unit tests for the pure helpers, and corrected docs/config.md's keymap exceptions. Split `main.rs` 9447→4909 lines into focused modules: `render.rs` (all UI + the `Render` impl), `commands.rs` (registry + keymap + `?`/`:` metadata), `navigation.rs` (cursor/selection/folding), `input.rs` (key dispatch + prefix state machine + palette). The remainder is the cohesive view core (state, refresh, row-building, staging/actions, transients, screen loaders).
- [x] Add a spinner to indicate when background activity is taking place, maybe in the top right of the title bar. It should only appear if the background activity takes longer than a certain threshhold (not user configurable). I'm not sure what the standard is for this type of delay but maybe 200ms is reasonable? — done: a subtle `gpui_component::Spinner` at the title bar's top-right, shown only after 200ms of continuous background activity (`BUSY_SPINNER_DELAY_MS`). An activity counter (`begin_activity`/`end_activity`) tracks in-flight ops; the first arms a generation-scoped delay timer, the spinner clears at zero. Instrumented at the background funnels — status refresh, the mutating-job runners, auto-fetch, diff loads — with `end_activity` placed before any stale-generation early return so the count can't strand the spinner.
- [x] Change the "Open config file" to say "Open global config", and improve the spacing of them. It's currently a little awkward. Also make them outline style — done: relabelled to "Open global config" (pairs with "Open repo config"), switched both to outline style so they read as distinct controls, and regrouped the header so the two config buttons sit tightly together with a wider gap before the unrelated "close" action.
- [x] Show a scroll bar on the settings screen in the same manner as the other views — done: wrapped the settings body in a full-width scroll container with a tracked `ScrollHandle` on `SettingsState` and added `.vertical_scrollbar`, so the bar sits at the window edge with the same theme-driven (`ScrollbarShow`) visibility as the status/log views.
- [x] All settings should reload on the fly when the config files are edited. E.g. font doesn't right now — done: font/ui_font/theme/appearance already live-reloaded through the config watcher → `apply_config` (verified against an external edit: the status view, the settings screen's dropdowns, and the commit editor all repaint, so the "font doesn't" note was stale). The real gap was settings whose effect comes from *fetched* data, not just how it's painted — the title-bar `Tag:` segment, which `[status]` sections are populated, and `[status].recent_count`. `apply_config` now `refresh()`es when `show_tags`/`status` change so they take effect live, and the in-app `toggle_control` grew a `refetch` flag (the "Tags in title bar" toggle uses it) so the Settings switch does the same instead of only repainting.
- [x] App should run in background when launched from command line, not block like it does now — done: `main` re-spawns a fresh child with `MAGRITTE_FOREGROUND=1` and null stdio, then the parent returns so the shell gets its prompt back. Avoid continuing a forked/`setsid` process into AppKit — that path caused macOS ViewBridge/BackBoardServices to spin at ~140% CPU while idle. `--foreground` / `MAGRITTE_FOREGROUND` still opt out for logs/debugging (the debug harness sets the env so it keeps the pid, log, and control channel).
- [x] Currently if I open magritte in a large repo it takes a while to load. It has to fetch information for every status section. Are we doing this in the most efficient manner we can? Can we do them independently, and show loading indicators for each section? Ideally we'd prioritize the main status sections — done: `refresh()` ran every section's fetch serially in one background task and updated the UI only at the end, so nothing showed until the slowest finished. Split it: a priority `git status` (+ in-progress sequence) renders the main file sections the moment it lands, then each auxiliary listing (unpushed/unpulled, recent, stashes, ignored, tags) runs as its own concurrent fetch and streams in independently — a slow one (the worktree-scanning `ignored`, or `describe`) no longer blocks the rest. A first-load section has no row yet, so it just pops into place when its data lands (no flash). A section that's *already on screen* shows a small spinner by its header while it's being re-fetched, gated on the same delay as the global busy spinner (`BUSY_SPINNER_DELAY_MS`) so a fast refresh never flashes it. Prior data stays visible across refreshes, and pushremote sections fetch only once status confirms a distinct push target. New `spawn_fetch`/`spawn_status_fetch` helpers, a `loading_sections` set, and a per-section `refreshing` flag; verified live (Recent commits rendered while a slowed Stashes popped in; on refresh the visible Stashes showed a spinner only after the delay). Caveat: warm, each git op is ~0.02–0.04s, so the gain is mainly cold/large-repo responsiveness and feedback, not raw wall-clock. (We first tried a per-section "Loading…" row, then switched to pop-in + the delayed refresh spinner.)
- [x] The command log hides some internally generated commands, but doesn't seem to hide some of the log or stash listing commands. — done: `GitCommand::is_query()` (the `$`-log filter) whitelisted the read-only UI queries but missed `git log` (unpushed/unpulled/recent, the log view, reflog, author autocomplete) and `git stash list` (the Stashes section), so they leaked into the default log. Added `log` to the query list and `stash list` as a special case (the mutating stash verbs — push/pop/apply/drop/show — stay visible); user-typed `!` commands still always show (the `user` flag short-circuits). Unit-tested; `a` ("show all") in the `$` log still reveals them.
- [x] Debounce the refresh on focus feature a little if we don't already — done: refresh-on-focus was firing on every window activation. It now throttles — refreshes immediately on focus, but skips if any refresh (manual `g r`, a post-action refresh, an auto-fetch, or a prior focus) ran within `FOCUS_REFRESH_COOLDOWN_MS` (5s). A `last_refresh` timestamp stamped at the top of `refresh()` gates it, so rapid app-switching — or macOS firing several activation events for one focus change — no longer re-runs a full status each time. (Tried a trailing debounce first; switched to immediate-on-focus + cooldown.)
- [x] Make the global background activity spinner slightly more prominent (maybe add a background to it?) — done: the title-bar activity spinner now sits in a rounded-square chip with the same shape and fill as the branch-name chip (`rounded(4px)` + `selection` background), so it reads as a deliberate indicator instead of a bare glyph blending into the bar.
- [x] The scroll bar on the settings screen doesn't behave properly — done: it was a bare `overflow_y_scroll` div with `vertical_scrollbar(&handle)` on the *same* element, so the scrollbar layer nested inside the scrolled content and the plain `ScrollHandle` never fed the thumb a content size (no thumb, mispositioned). Switched to gpui-component's `Scrollable` wrapper (`overflow_y_scrollbar()`), which manages the scroll area + a correctly-sized scrollbar itself; the outer `w_full` keeps the bar at the window edge while the content column stays capped. Dropped the now-unused `SettingsState.scroll` handle. Verified the thumb renders at the window edge (forced `ScrollbarShow::Always` during testing).
- [x] We should prevent entering text on the commit screen if the confirm quit y/n prompt is active — done: while the "Discard message?" confirmation is up, `on_capture_key` only stopped propagation for `y`/`n`/`esc`, so any other key fell through to the message input. It now `stop_propagation`s *every* key while confirming (only y/n/esc act), so typing can't edit the message behind the prompt. Verified live (typing "ZZZ" at the prompt left the message unchanged).
- [x] "Nothing to commit, working tree clean" should show above other status sections — done: `rebuild_rows` appended the clean notice after the section loop (below stashes/recent/log). Moved it to lead the list — emitted before the sections when `status.is_clean()` — so it sits at the top with the listings beneath. Verified live.
- [x] Vim mode for commit editor? — done: opt-in `commit_vim_mode` (settings toggle + config), built as designed in `docs/dev/vim-mode.md`: a pure in-house engine (`crates/magritte/src/vim/`) layered over `InputState` with Normal/Insert/Visual modes, counts, the full motion set (`h j k l 0 ^ $ w W b B e E gg G f/t/;/, { } %` + Enter/space/arrows), `d`/`c`/`y` operators with Vim's exclusive-motion adjustments, `iw`/`aw`/quote/bracket text objects, the surround MVP (`ysiw"`, `yss`, visual `S`, `cs`, `ds`), registers/put with linewise semantics, `x X r ~ J D C Y S s u ⌃r`, `.` repeat (surround and Insert sessions included), literal `/`//`?` search with `n`/`N` and live match highlighting, engine-native per-command undo (`u` after `dw..` undoes one step; an Insert session is one unit), the `_` motion, `gq` as a range reflow operator (`gqq`/`gq{motion}`/visual `gq`, summary always protected), evil's `,,`/`,c`/`,k` with-editor leader alongside `ZZ`/`ZQ`, a vim-style mode line between the message and the diff preview (mode chip + pending keys / live search prompt), and a self-drawn selection overlay + block cursor (`range_to_bounds`). Editor commands use evil's commit-buffer keys — `ZZ` commit, `ZQ` cancel, `gq` reflow — so Esc stays free for modal editing. Focus is the routing switch: Insert focuses the input, Normal/Visual focus the view (no caret, no Input-binding preemption; the capture phase owns every key). Semantics verified against headless Vim 9.2 probes and locked in by ~400 table-driven engine tests plus an LCG fuzz. Later (in the doc): marks/registers, regex search, `:` line, mouse-Visual integration. The external-editor path (`commit_editor = "nvim"`) remains the full-fidelity route.
- [x] Don't write an empty `command = []` to the config file, as if a user tries to add `[[command]]` later, it causes an error until that's deleted — done: added `skip_serializing_if = "Vec::is_empty"` to the `commands` field, so a saved config omits `command` entirely when there are none (a non-empty list still writes `[[command]]`). Unit-tested both cases.
- [x] When switching back to a window with an expanded diff, the auto refresh on focus causes the diff to disappear briefly. Let's make it so that doesn't happen. In general we should avoid repainting content with "loading", and just update it to the new state after it has loaded — done: refresh now retains already-loaded expanded file diffs/highlights and force-reloads them in the background, preserving the old body until the replacement lands instead of flashing "Loading…".
- [x] Adopt cargo fmt — done: stock rustfmt (measured as the least-churn config against the existing hand formatting), a one-time reformat commit, and AGENTS.md now asks for `cargo fmt` before committing instead of warning against it.
- [x] The scrolling of the settings screen is still off, fix it — done: the settings screen now has an explicit scroll handle with the scroll area constrained to the flex slot below the title bar, and the scrollbar is rendered from the outer layer like the status/log views.
- [x] Scrolling is laggy. This is not acceptable. It needs to be completely smooth. — done: this appears to have been another symptom of the bad default detach path spinning AppKit/ViewBridge at ~140% CPU; after switching background launch to a fresh child spawn, idle CPU settles near zero and scrolling is no longer fighting that load.
- [x] Related to the above, CPU usage seems crazy high. Even when the app is doing nothing in the background. — done: the high idle CPU was specific to the default detach path (`fork` + `setsid` + continue into AppKit), not status rendering. Replaced it with a fresh child spawn; verified `target/debug/magritte ~/dotfiles` idles at ~0–0.5% after settling.
- [x] Package/distribute Magritte — done for v0.1.0: app launches are single-instance/multi-window via a per-user IPC socket; a repeated `magritte <repo>` hands off to the running app, opens a new repo window, or focuses an existing repo window. Added macOS bundle pieces (`co.lyall.magritte` Info.plist template, `scripts/dist-macos.sh` to build/ad-hoc-sign/archive `Magritte.app`) and best-effort Linux packaging (`scripts/dist-linux.sh`, `.desktop`, GPUI `wayland`/`x11` features). Tagged release CI builds macOS/Linux artifacts, publishes public Homebrew tarballs to `lyallcooper/homebrew-magritte`, renders/commits `Formula/magritte.rb`, and installs successfully via `brew install lyallcooper/magritte/magritte` (with Homebrew 6 tap trust). Still nice-to-have: real `Magritte.icns`/Linux PNG and notarization/developer-id signing if we later get an Apple developer account.
- [x] We should try to minimize the amount of rewriting of user config that we do. It's unsettling when the user edits their config file then the app moves or changes the syntax of what they've written due to deserializing then serializing — done: Settings saves now patch only the top-level fields the Settings UI owns using `toml_edit`, preserving comments/order/custom command tables/transient/keymap syntax elsewhere. "Open global config" creates an empty file if needed instead of serializing the whole config, and closing Settings no longer writes the merged config back out.
- [x] In the log view, in large repos where the short shas get longer, the sha column overflows — done: the log view measures the longest abbreviated hash in the loaded entries and sizes the hash column to fit, so repos with larger `core.abbrev` values keep the subject/date columns readable.
- [x] Commit details should show the full commit message, not just the title — done: commit detail loads the full commit message (`%B`) and shows the body above the diff (the subject remains in the header).
- [x] Users should be able to save switches that have arguments in them (e.g. log's -n) — done: transient-save now treats option values as part of the saved argument state. Saved files keep their existing switch array format and add options as `key=value` entries (e.g. `log = ["-r", "-n=50"]`), restore them when reopening the transient, and show the save hint when either toggles or values change.
- [x] People may not know what "dwim" means. Let's use a different term. — done: the user-facing diff action is now "smart" / "Diff smart target"; the internal command variant remains `DiffDwim` because it still describes the Magit concept.
- [x] Add other push transient arguments from magit — done: the push transient now includes Magit's remaining simple argument switches that apply to our existing push flows: force, disable hooks, set upstream, include all tags, and follow annotated tags.
- [x] We should dynamically show the relevant commands from the '?' menu depending on what view the user is in and where the cursor is. How does magit handle this if at all? — done: Magit keeps a global dispatch plus section/view-local keymaps; Magritte now mirrors that pragmatically with a context-aware help menu for status-at-point, log, commit detail, diff, command log, and rebase todo views.
- [x] Clicking outside of a transient to dismiss it shouldn't also trigger an action (eg expanding/collapsing a diff) — done: row mouse-down/click handlers now treat an open popup as a dismiss-only click and return before selecting/toggling/acting.
- [x] Binding in commit detail view to show even more commit details — done: commit detail now has `a` (and a header button) to toggle author/committer/date/ref metadata above the message and diff.
- [x] Our diff dwim acts different from magit in the unstaged section — done: smart diff now follows Magit by treating staged/unstaged section headings and their child file/hunk/line rows as whole-section diffs, without inventing a pathspec from the row at point.
- [x] Add a way to check if the current version is the latest — done: added `magritte --check-version` and a palette-only "Check for updates" command. Both query GitHub's lightweight latest-release API on the public distribution repo (`/repos/lyallcooper/homebrew-magritte/releases/latest`) with a short timeout, parse the published `tag_name`, compare it with the built package version, and report whether an update is available.
- [x] `magritte --version` should output the version — done: `magritte --version` / `-V` now prints `magritte <version>` before single-instance handoff or GUI detach. Also corrected the app crate metadata to `0.3.0`, so package metadata, `--version`, and dist script defaults agree with the current release line.
- [x] Expanding and collapsing the details in the commit view makes an extra blank line be added above the diff each time — done: details insertion now removes an existing leading blank separator before adding metadata + one separator, so repeated `a` toggles keep exactly one blank line between details/message and the diff. Unit-tested the regression.
- [x] Does magit confirm before reverts? Also we don't have keybinds set up for the continue/skip/abort of a revert — done: Magit does not confirm before starting a revert, so Magritte still starts reverts directly. In-progress sequence prefixes now mirror evil-collection-magit/Magit: `A` opens cherry-pick continue/skip/abort, `_` opens revert continue/skip/abort, `r` opens rebase continue/skip/abort, and `m` opens merge abort.
- [x] When we ask a yes/no question, we don't need the "(y/n)" after the question mark. The <y> and <n> in kbds are sufficient — done: confirmation prompt text now omits literal `(y/n)`; the bottom bar's clickable `y`/`n` keycaps remain the answer affordance.
- [x] Periodically check if there is an update, and alert the user if there is. If the update check fails, do so gracefully without the user noticing. Add an option to disable on the settings screen — done: a background loop checks GitHub's latest-release API after startup and then daily, silently ignores failures, and shows one transient notice per newer version. Added `check_for_updates` (default on) plus a Settings toggle that starts/stops the loop immediately.
- [x] The app always opens in the center of my primary screen. That doesn't feel very macOS native for some reason. Can we do better? — done: app-owned state now goes through a small typed TOML state module. Magritte persists window placement as `window.toml` both per-worktree (`.git/.../magritte/window.toml`) and globally (next to `config.toml`), including `mode` and `display_uuid`; launch restores per-worktree first, then global fallback, clamped to visible displays. First launch uses a 1000×720 window inset from the visible top-left instead of exact center, and additional windows cascade from the active Magritte window.
- [x] Add timings to the command log so slow git calls are visible (e.g. `42ms git status`, `1.4s git diff`) before we decide what to cache or optimize. Highlight lines that took an especially long time. — done: every recorded git/user command now carries wall-clock elapsed time, and the `$` command log renders a fixed-width duration column before the command (`42ms git status`, `1.4s git diff`). Calls over 500ms use the warning color; calls over 2s use the error color.
- [x] Avoid duplicate git queries within one refresh, likely by introducing a per-refresh snapshot for common metadata (HEAD/current branch/refs/config bools/sequence state) that status sections and render paths can share. — done: added a core `RefreshSnapshot` as the priority refresh boundary, carrying parsed `Status` plus in-progress `Sequence`. The UI now fetches that snapshot instead of issuing separate `status()`/`sequence()` reads, and the sequence reader can reuse an already-resolved git dir for snapshot-style callers. Expanded after command-log audit: status now carries the resolved `@{push}` remote, remote transients build targets from cached status instead of re-running current-branch/upstream/push config queries, config-backed transient switch defaults are cached for the current refresh generation, and refresh snapshots reuse the startup-resolved worktree git dir so `rev-parse --absolute-git-dir` is not repeated on every refresh. Kept auxiliary `@{upstream}`/`@{push}` listings on Git's revision syntax rather than hand-expanding refs, to preserve Git's edge-case resolution.
- [x] Add an in-memory cache for immutable commit/blob-derived data, starting with commit details/diffs keyed by full OID and possibly highlighted blob fragments keyed by blob/content hash + language/query version. — done: commit detail loads now use a bounded in-memory cache (64 entries) keyed by revision plus diff args/pathspecs, storing metadata, full message, parsed file diffs, and detected languages. Opening the same commit detail again re-renders from cached immutable data using the current theme instead of re-running the git metadata/message/diff calls. Deferred blob-fragment highlight caching until the language/query/theme versioning boundary is cleaner.
- [x] I think .gitignore paths should begin with a '/' — done: ignore prompts now seed concrete path rules with a leading slash, matching Magit (`/path` for top-level/private/global ignores and `/basename` for subdir `.gitignore` files), so generated rules are anchored instead of matching the same name anywhere below the ignore file.
- [x] Vanilla emacs/magit base keymap option (non-evil-collection-magit) — done: added `keymap_preset = "evil-collection" | "vanilla"`, with evil-collection remaining the default. The selected preset is now the source of truth for top-level defaults, secondary aliases, transient suffix keys, command-palette/help key hints, and commit-row/log revert keys; user `[keymap]` overrides still apply on top. Documented in `docs/config.md` and unit-tested representative evil vs vanilla bindings and suffixes.
- [x] Full Magit-style `!` run transient — done: `!` opens magit's run transient (`!`/`p` git in root / file-at-point's directory; `s`/`S` shell via `sh -c` in root / working directory, so pipes and `&&` work); `|` (evil) and `:` (vanilla) stay the direct git prompt. Core `run_user_in`/`run_shell_in` + test.
- [x] Remote transient variable/configuration parity — done: `url`/`fetch`/`pushurl`/`push`/`tagOpt`/`followRemoteHEAD` shown inline and editable via the `C` configure sub-transient (magit's keys: `S` push refspec free-text, `h` followRemoteHEAD cycling create/always/warn).
- [x] Which-key should expand to multiple rows as needed so it doesn't overflow off the right edge — done: the strip is `flex_wrap`, so a prefix with more continuations than fit one line wraps onto further rows.
- [x] Why do some motion commands appear in the command palette but others don't? E.g. jump to untracked vs jump to ignored — answered: it's the palette's enabled-gating (the direction #52 wants more of). Every `jump-to-*` command shares one definition; a jump is listed only while its section is on screen, and the Ignored section is opt-in (off by default). Documented in docs/config.md.
- [x] We can improve the settings screen layout a bit. E.g. compacting some switches onto a single line as space allows. — done: the Behavior toggles and the commit editor's ruler/wrap pair now pack onto wrapping rows instead of one full labelled row each.
- [x] Codebase review + refactor (second full pass) — done: read every line of both crates again. Fixed four verified bugs (nested `command` keys corrupted by the config deep-merge; `succeeds`/`run_optional` bypassing cancel/timeout; `current_branch` failing on unborn repos so push/pull/fetch couldn't open; stale commit-editor diff loads landing in a newer editor), then a core DRY pass (shared argv builder, one apply runner, unified todo parsers, `GitOutput::lines`), pruned the transient model (dead `TagAnnotated`, the one-variant `Repo::execute` router and its label tables, `Action::suffix` collapsing 68 five-line constructions), +17 tests for untested ops (all ResetModes, merge/pick success paths, tags_around, plain rebase, --edit-todo, config helpers), split main.rs 6.8k → ~1.7k lines into nine sibling modules (commit_editor, log_view, commit_diff_view, transient_state, staging, status_loader, row_build, watchers, window/palette), unified CommitView/DiffView around a shared FlatDiff, deduped the act-at-point key handling that had already drifted between the key and menu paths, and a perf pass: memoized `$`-log rows (were re-flattened twice per frame), Arc-shared parsed diffs and Rc-shared highlight spans (staging a line no longer deep-copies the file diff; rebuilds bump refcounts instead of copying spans), per-file `git status` calls removed from unstage/discard, and debounced settings saves (were writing the config file per keystroke).
- [ ] Work through docs/magit-parity.md — the full magit feature/behavior audit (every transient, flag, motion, section, act-at-point verb, and both keymaps, each marked parity/differs/partial/missing). Its Recommendations section is the prioritized build list; it also records the deliberate deviations to keep.
- [x] Fixup/squash workflow (magit's commit `f`/`s`/`F`/`S` + rebase `f` autosquash) — done: the commit transient's Edit column creates `--fixup=`/`--squash=` commits targeting the commit at point or a log-selected one; the Edit-and-rebase column's instant variants create + autosquash immediately (warning first if the target is published); `r f` autosquashes existing fixup!/squash! commits since the upstream merge base. Core `commit_fixup`/`commit_squash`/`rebase_autosquash`/`upstream_merge_base` + tests.
- [x] Behavior/friendliness batch — done: rebase `-A`/`-a` keys match magit; mutually-exclusive transient switches (cherry-pick `--ff`/`-x`, merge `--ff-only`/`--no-ff`); stash prompts for a message and the drop key follows the preset; `S` stages tracked-only (`git add -u`) and `S`/`U` confirm when they'd blur the staged/unstaged split, with section-header stage/unstage verbs; discarded files go to the system trash. Parity doc + docs/config.md updated per fix.
- [x] For recent commits, we show local branch, upstream/branch and upstream/HEAD, sometimes all on the same commit. Magit doesn't show upstream/HEAD and it shows upstream/branch colored in such a way to show that it matches the local branch. I prefer that approach, where we combine the two via the established consistent styling rules when they're equivalent — done: `parse_refs` now drops remote `*/HEAD` pointers and, when the current branch and its upstream both decorate a commit, folds them into one synced entry (`RefKind::SyncedHead`) rendered as `remote/branch` with the remote prefix in the remote color and the branch in the current-branch color. Also unified ref styling across the commit-list code paths (a first consistency pass): added a `branch_local` (blue) palette color and one `ref_chip` renderer used by both the status commit rows and the log rows — local branch blue, remote green, tag yellow, current branch bold. Previously the status rows colored remotes blue/locals green and the log view dumped all refs as one unparsed blue blob. Follow-up (also done): branch/tag/remote pickers now color candidates by kind (style derived from the picker's action — branch pickers classify local blue / remote green, tag pickers yellow, remote pickers green), and the commit-detail "Refs:" metadata line renders colored ref chips instead of a dim string. So ref styling is now consistent across the title bar, transients, commit/log rows, commit detail, and pickers.
- [x] Change the which-key layout to be column oriented instead of row oriented. I.e. grow the list vertically until it gets too tall, then split into an extra column. Check how emacs which-key does this and check how our transient layout engine works, as it may be applicable — done: `prefix_indicator` now lays the continuations out column-major (like emacs which-key) instead of as a wrapping row — each column fills top-to-bottom until it would exceed ~a third of the window height (`viewport / 3 / ROW_HEIGHT`), then wraps into the next column. The typed-keys chip sits to the left of the grid.
- [x] I want to try using the ⏎ symbol instead of "Ret" to indicate return inside a kbd. The problem is that the symbol renders poorly in many fonts. Can we render it using the system UI font? Or will that cause layout or UI issues if it's not the same width as the other monospace characters — done: keycaps now show `⏎` for Return, drawn in the UI font (`kbd::key_chip`/`chord_caps` gained a `ui_font` param, threaded from `self.ui_font`), since the glyph is thin/tofu in many monospace fonts. No layout issue: each keycap is an independently-sized flex box (`min_w` + centered), so the glyph's width doesn't need to match the mono grid.
- [x] Context-scoped keymap/command architecture — done: promoted every secondary screen's verbs into the `commands()` registry with a `contexts: ScreenSet` field (which screens a command dispatches in), so the keymap is now per-context (`ScreenKeymaps = ScreenKind → keymap`) and a key can mean different things per screen without collision. One `dispatch_key` routes `(screen, key) → command` for refs/worktree/log/commit/diff/rebase-todo/git-log, replacing the per-screen `on_key` branches, the `run_info_key` twins, and `log_commit_key`. The `?` menu (`dispatch_menu_for`) and the header hints (`header_action`) are now *derived* from the same per-context table — resolving each verb's key from the live keymap — so menu, headers, and dispatch agree by construction and reflect the preset/remaps (fixes the recurring drift where a secondary screen showed the wrong key or the status menu). Scoped verbs win their own context over global motions (rebase-todo `p`=pick beats the vanilla `p`=prev-section). A per-screen invariant test (`every_scoped_verb_dispatches_to_itself`) pins it for both presets. Follow-up (also done): brought the status act-at-point verbs into the model too. The per-context keymap now maps a key to an ordered *candidate list* (`KeyBindings = key → Vec<id>`), sorted most-specific-first via `command_priority` (at-point verb > screen-scoped > global); dispatch (`resolve_binding`) runs the first candidate whose `enabled` holds, so `a` = cherry-apply on a commit, stash-apply on a stash, and Stage on a file — all in the status context, gated by an `at_point`-marked command's target check (`point_commit`/`point_stash`). `point_commit_key`/`point_stash_key` deleted; the status "Commit/Stash at point" `?` groups are now derived from the enabled at-point verbs; copy-hash/ref folded into `copy_at_point`. The per-screen invariant and per-context uniqueness tests were extended to allow (and check) shared keys. Supersedes the pragmatic context-aware menu from the earlier "#132" item. (Also fixed a latent bug from the first pass: `refresh`/`yank` had narrowed to status-only, so `g r`/copy stopped working on secondary screens — both back to `ScreenSet::ALL`.)
- [x] Clicking on a row/button/etc when the window is out of focus should not trigger the expand/collapse/button action/etc — done: a root-level `capture_any_mouse_down` handler swallows the macOS first-mouse (window-activating) click in the capture phase (`cx.stop_propagation()`), before any element arms its click, so an out-of-focus click only focuses the window. One place covers every row/button/dropdown.
- [x] Blame view — done: `git blame --porcelain` parsed into per-line annotations (`Repo::blame` → `BlameLine` {short, author, date, line_no, text, group_start}); a `Blame` pager screen (scrollable, no cursor, like the `$` log) shows a commit·date·author gutter (once per commit run) + line numbers + content, opened via the `:blame` palette command on the file at point, closed with `Esc`/`q`. Core-tested; verified live.
- [x] Bisect — done: banner-driven like the sequence UI. `Repo::bisect`/`bisect_start`/`bisect_mark`/`bisect_reset` (state read from `BISECT_LOG`); a `B` transient starts a bisect (pick a known-good commit in the log; `HEAD` is bad) or, while bisecting, marks the checked-out commit good/bad/skip or resets. A "Bisecting" banner shows the decisions + `B g`/`B b`/`B s`/`B r` controls. Core-tested; verified live start→good→reset.
- [x] Patch / `git am` — done: a `W` patch transient — create (`format-patch` a range), apply a diff to the worktree (`git apply`), or apply a mailbox as commits (`git am`, pausing into the existing am sequence banner on conflict). `Repo::format_patch`/`apply_patch_file`/`am_patch`; free-text prompts (like `!`). Core roundtrip-tested; create verified live.
- [x] Since the '?' command works correctly in detail views now, we can remove the kbd buttons from the top of the window. But if the user is supposed to take an action (eg select a commit to rebase since) then we should still show the kbd buttons — done: the commit/diff/refs/worktree/command-log/blame views (and the log while browsing) show just their title; the log in select mode still shows view/select/cancel and the rebase-todo keeps start/cancel.
- [x] In the commit detail view, files and diff hunks should be collapsible (like magit). We should also show commit stats overview between the commit message and the first file (N files changed, K insertions(+)...). The file names should also match the styling from the status view (modified file.txt) — done: diffstat overview line, status-style file headers (colored change word + path), and a `FlatDiff` fold model (collapsed-header set + visible-row projection; Tab/click toggles; the apply engine still walks the full rows).
- [x] The app icons should be a squircle like other macOS apps, not just a rounded rect — done: `make-icns.sh` masks the icon to a superellipse (the continuous-curvature macOS squircle) instead of a circular rounded rect; masters + `Magritte.icns` regenerated.
- [x] Opening a file in an editor should open to the first changed line of the hunk, not the start of the hunk — done: `Hunk::first_change_new_line` (first added line's new-side number, or the hunk start for a delete-only hunk); used for file/hunk targets.
- [x] Show the blame inline instead of to the side, like magit. I.e. insert a new line between the lines of the file with the blame details — done: a full-width commit annotation (sha, author, date, summary) is inserted above each commit run, with the file lines beneath.
- [x] Ret key icon should just be in the UI font set by the user, but in the system UI font to ensure a quality glyph — done: the `⏎` glyph (and `⇥`/`⎋`/`⌫`, plus the `⌘`/`⌥`/`⌃`/`⇧` chord glyphs) render in the platform system UI font, independent of the user's configured `ui_font`.
- [x] Bisect should work by having the user enter the revisions instead of select from log, like magit — done: bisect start prompts for the known-bad revision (default HEAD) then the known-good revision (two chained free-text prompts), replacing the log-select flow.
- [x] Cap which-key at 1/4 window height instead of 1/3 — done.
- [x] Do we validate user keymap keys from the config file? — done: `kbd::keystroke_error` rejects empty steps, `+`-joined chords, and unknown modifiers when the keymap is built from config.
- [x] Mouse should work on commit detail/diff view — done: clicking a row moves the diff cursor (so the apply engine acts on it); clicking a File/Hunk header also toggles its fold.
- [x] Sections/files/hunks should be collapsible in commit detail/diff view — done: the `FlatDiff` fold model (Tab or header click toggles a File/Hunk fold; folds are independent).
- [x] Commit detail view file change stats should also show the per file "4 +++-" stats — done: file headers show `N` total changed plus a scaled green `+`/red `-` bar.
- [x] Diff view should show file change stats — done: the diff view leads with the same diffstat overview and per-file stat bars as the commit view.
- [x] Double click with mouse should trigger the enter action on a row — done: a double-click on a status row invokes its Enter action.
- [x] On screens such as interactive rebase log view, where the user is prompted to select a commit, let's change the view keybinding to Space (from Return), and the select keybinding to Return (from Cmd-Return) — done: in select mode Space views the commit at point and Return confirms the selection.
- [x] Clicking on an already selected row should also trigger the enter action. Maybe this can supercede our double click logic since double clicking should be equivalent to selecting the row then clicking again — done: a click on the already-selected row fires Enter; the double-click path folds into this (its second press lands on the selection).
- [x] The "x" in "⌘x" should still be rendered in the user's selected font — done: keycaps draw the modifier glyphs in the system UI font but the key itself in the monospace keycap font.
- [x] I think there's a bug with custom bindings that have shift + a key like esc or tab in them. They don't seem to work properly. — done: `chord` keeps Shift as an explicit prefix on named keys (`shift-tab`/`shift-escape`), and Shift-Tab is overridden from gpui's reverse-focus-nav so its binding can dispatch.
- [x] Shift+space should be treated as a different input from space alone, same for other modifier keys + special keys — done: Shift on a named key stays an explicit modifier, so `shift-space` is distinct from `space`.
- [x] Change Escape back to being displayed as "Esc" — done: Esc spells out again (the other named keys keep their glyphs).
- [x] We should validate keybindings such that "abc" isn't valid — done: a base must be a single character, a named key, or `f1`..`f12`.
- [x] "Esc", "Escape", "Ret", "Return", "Space", "SPC", "Tab" etc should be valid ways of spelling the keys in keybindings — done: named-key aliases are accepted and normalized to the runtime form.
- [x] Command palette should show command id (in italics?) after the keybinding to help in id discovery for user keymaps — done: the id shows dim and italic at the row's end.
- [x] All views that have a Esc to cancel/go back binding should show the Esc kbd button in the top right to support mouse usage — done: secondary views show a clickable `Esc close` keycap at the title bar's right.
- [x] Transient layout should try to take window size into account to ensure that nothing overflows off the right edge of the window (eg log's arguments do right now) — done: the argument band caps its columns to what fits the window width, fanning into more rows instead of overflowing.
- [x] If a binding is invalid, eg "abc", don't save it to the internal keymap at all so it doesn't show up in transients etc — done: an invalid spec is warned about and dropped, never inserted.
- [x] Log view's +/- bindings work, but it still shows "first 256" regardless of the actual count displayed — done: the header shows the current `log.limit`, not the default constant.
- [x] Pressing an unbound key while the help transient is open should dismiss the help and show the usual "<x> is unbound" — done.
- [x] Pressing an unbound key while in a specific view should show "<x> is unbound in <view name> view" — done: secondary views report the unbound key with the view name.
- [x] Keep the Esc to close out of the title bar and in the top right of the main window — done: the `Esc close` button is a top-right overlay in the main window, not in the title bar.
- [x] The reactive window width layout for transients is an improvement, but we're not measuring the widths exactly right it seems. Sometimes the content can still overflow — done: each group's column width is now measured from its widest suffix (keycap + description + flag) instead of a fixed guess.
- [x] There is an unnecessary gap on some views between the bottom of the content and the bottom of the window. The status view does it correctly. — done: secondary view containers fill with `flex_grow` like the status list, rather than `h_full`.
- [x] The "x is unbound" message should be dismissed automatically after timeout or if another key is pressed — done: it already fades on a timeout, and now the next keypress clears it too.
- [x] The log view still says "first N" even if the log viewing is reversed (-r). Does this make sense? — done: with `--reverse` the header says "last N" (the same most-recent N, shown oldest-first).
- [x] The help '?' button shouldn't allow click to fall through it — done: the button is `occlude`d.
- [x] The help button should have a subtle opaque background so it doesn't clash with text below it — done: it has a subtle opaque fill and a faint border.
- [x] Commit edit tooltips about summary being too long etc should be opaque — done: the theme's `warning.background` (which gpui-component paints the diagnostic popover with) is now an opaque popover surface, and the banner keeps its wash via a code-side alpha.
- [x] Commit editor diff view should be mouse enabled, collapsible sections, etc — done: File/Hunk headers in the preview show fold triangles and collapse on click (shared fold model with the flat-diff views).
- [x] The diff stats (+++-- etc) should appear all together before the files, not with each file header (like magit does) — done: a diffstat block (per-file `path N +++---` lines + summary) sits above the diffs; file headers show just the change word and path.
- [x] Line highlight and select is not visible over green/red diff rows in commit/diff detail views — done: the cursor/visual highlight wins over an added/removed row's tint.
- [x] Instead of the text "Tag: " before the tag in the title bar, can we use a tag symbol? — done: a tiny vertical "TG" letter label (tinted the tag color) replaces the "Tag:"/"Tags:" text; the same `stacked_letters` helper is available for other markers.
- [x] Add (brief) mouseover tooltips for the various titlebar items describing what they are — done: the repo name, branch chip, push/upstream chunks, ahead/behind pills, tag, and dirty marker each get a hover tooltip (shared `titlebar_tip` helper + a `tip` on `titlebar_action`).
- [x] Instead of our TG glyph to mark the tag in the title bar, pillify the tag text. Include the commits since tag count inside the pill in a nested, smaller pill, maybe styled as an outline pill only. — done: each tag renders as a tag-tinted pill holding the name with the commits-since count as a nested outline pill (new `outline_pill`); the `stacked_letters` "TG" glyph is gone.
- [x] Enter from the ref browser shouldn't try and checkout everything — done: Return now *visits* the ref (opens its tip commit's detail over the browser, magit-visit-ref's default); checkout moved to an explicit `b`. The tip resolves off the UI thread.
- [x] Commit details view should show the details by default, but as a collapsed by default — done: the metadata now always leads the view under a foldable "Details" header (collapsed by default; `=`, Tab, or a click toggles). The expanded/collapsed preference persists per worktree in folds.toml, so it carries across commits and restarts — per-repo, not per commit. Row indices no longer shift on toggle, so cursor/selection state survives.
- [x] Commit details view should say Commit <sha> — done: the commit view's body now leads with a selectable "Commit <full-sha>" head line (magit's revision buffer); the redundant short-hash header chip is gone.
- [x] Commit details view sha should be char-wise selectable — done: the head line is a normal selectable row, so the sha drag-selects and copies like any other text.
- [x] Clicking anywhere should dismiss the current selection, not just when other selectable text is clicked — done: a root bubble-phase handler clears the active char/visual selection on any left click that didn't land on a selectable row (empty space, chrome, a section header).
- [x] Add config support a la magit's branch at remote (and whatever other) transients — done: a new `Suffix::Variable` (choices-cycle or free-text-prompt) plus `branch-configure`/`remote-configure` sub-transients reached via `C` in the branch/remote transients. Branch: description, rebase, pushRemote (+ repo defaults pull.rebase, remote.pushDefault); remote: url, fetch, pushurl, tagOpt. Rows show the live value (and any inherited fallback); choices cycle in place, free-text prompts seed the current value; both write to git config (new `Repo::config_set`/`config_unset`) and refresh.
- [x] Gracefully handle when git is not installed and provide a brief but helpful error message — done: `Error::is_git_missing()` classifies a spawn `NotFound`; startup discovery keeps that reason so a missing `git` shows "git was not found. Install git or add it to your PATH…" instead of the misleading "not a git repository", and a mid-session disappearance shows the same.
- [x] Commit detail view "Commit <sha>" line should be at the top of the window, same line as the Esc button — done: the full sha now sits in the view header beside the close button (dim "Commit" + mono hash); right-click copies it (`y b` still does too). The body's Head row is gone, so the message starts the scrollable content.
- [x] Codebase review + refactor (third full pass) — done: four parallel deep reads (core, control flow, rendering, infra/docs) with every finding verified against the code before acting. Fixed core git bugs: staged renames diffed as whole-file additions (pathspec missing the original path — display wrong and hunk-level unstage applied the wrong patch), combined `diff --cc` records breaking/blanking the diff parser, C-quoted and trailing-tab paths, a panic on truncated `?`/`!` porcelain records, `.R` worktree renames classified as staged, `--no-autosquash` silently defeating `r f`, blame gutter missing on a commit's later runs, todo verbs without oids (`exec make test` → oid "make"), message-less release tags dropped, `git config` reads leaking into the `$` log, and missing `--` separators (a file named `-3` parsed as a flag). App fixes: mouse-toggled transient switches skipped the mutual-exclusion rule, overlapping jobs clobbered each other's cancel flag, ghost transients after early-return dispatchers, cmd-R swallowed during a paused rebase, stale selections surviving background refreshes (out-of-bounds highlight ranges), and the GitLog/Blame pagers snapping back after a wheel scroll. `+`/`-`/`0` became ordinary registry commands (remappable, palette-visible). Robustness: a bad repo config overlay now falls back to the global config instead of defaults, settings saves surface failures instead of silently no-oping, the repo config watcher survives the scope dir being created mid-session, and the IPC server can't be wedged by a stalled client. Dev tooling: dbg.sh kills only its own instance (per-dir pid), control dir/log under $TMPDIR, playground.sh refuses to rm -rf a directory that isn't a playground. Split render.rs (4.7k → 1.4k lines) into title_bar/transient_render/picker_render/list_render/diff_render. Docs synced (AGENTS.md structure, README packaging, config.md keys, magit-parity.md's shipped features). +20 regression tests; verified live via dbg.sh (rename diff, switch exclusion, `+` dispatch, log screen).
- [x] Show magit's "Unmerged into <upstream>" instead of "Unpushed to <upstream>" — done: the upstream divergence section now uses magit's heading (magit-insert-unpushed-to-upstream); after a rebase those commits may already be on the *push* target, so "Unpushed" lied. "Unpushed to <push>" remains the pushremote section's heading, matching magit exactly.
- [x] Review backlog follow-through (the third pass's deferred items) — done: unified the thrice-copied drag-selection state machine into one `DragState` the status rows / flat-diff views / log pack their fields into (the log gained the missing open-popup guard); split `controller.rs` (3k) into `jobs`/`transfer`/`rebase_flow` slices and core `transient.rs` into model + menu definitions; guarded the fire-and-forget probes (published-history check, tag-release seeds) so a raced result can't pop a prompt over whatever the user did next; sequence controls run their reword probe as one cancellable job (key bounce can't double-continue) and report when another operation blocks them; `prompt_branch`'s remote-branch listing and the Configure transients' git-config variable reads moved off the UI thread (values fill into the open popup); per-frame perf — memoized `FlatDiff::visible_rows`, `Arc<FileDiff>` through the commit cache/apply engine, log ref decorations parsed at fill time, an aggregate highlight budget for many-file commits, palette key hints resolved once per picker; deduped the four list-cursor moves into `list_move` and the five picker-seed sites; plus: auto-fetch timeout, unknown-key feedback inside transients, transient panel viewport cap + scroll, file-sized blame gutter, shell-quoted editor commands, subprocess poll backoff, confirm ignores modifier chords, dead core wrappers removed, inert --diff-merges dropped. Deliberately left as direction (per FEEDBACK.md): grouping StatusView's ~65 fields into sub-state structs and typed git-intent structs at the core boundary — both wide mechanical renames/API redesigns better done as their own focused pass.
- [x] Window placement doesn't remember monitors it seems — done: gpui window frames are per-display (a placement is display + display-relative bounds, with `WindowOptions.display_id` picking the display — primary when unset), but restore treated the saved frame as global coordinates and never passed a display id, so any window closed on a secondary monitor reopened on the primary. Restore now resolves the saved `display_uuid` to a display id and clamps within that display's visible area (primary fallback when the monitor is gone); the new-window cascade also follows the active window's display. Verified with a live round-trip on both monitors plus the missing-monitor fallback.
- [x] What macOS menu niceties can we add? It should feel like a macOS native app when we're on macOS — done (first pass, `menus.rs`): full standard menu bar — app menu (About, Check for Updates…, Settings…, Services, Hide/Hide Others/Show All, Quit), File (Open Repository… ⌘O with the native folder picker, Close Window), Edit (Undo/Redo/Cut/Copy/Paste/Select All carrying the OS edit roles, so macOS adds Dictation/Emoji and text inputs get the standard behavior; Copy falls through to copy-at-point on the status view), Window (Minimize ⌘M/Zoom, adopted as the system windows menu so macOS adds the window list + tiling), Help (opens the `?` dispatch menu). Plus a Dock menu with Open Repository…, and ⌘H/⌥⌘H hide bindings. Possible later niceties: recent-repos in the Dock menu (`add_recent_document`), a titlebar proxy icon/represented file for the repo path.
- [x] Sometimes we'll show two tags in the titlebar. I think if we have unpulled commits from upstream? — confirmed and fixed: the second tag was `git describe --contains HEAD`, which reports tags on commits you haven't pulled whenever the upstream is ahead. The title bar now shows only the nearest *reachable* tag (`Repo::nearest_tag`); the contains-tag is gone.
- [x] The commit editor sometimes loads before the diff is ready, leading to pop-in and shift after it loads — done: the message/diff split is reserved from the first frame whenever a diff is expected (`diff_expected`, known at open time; tag messages still fill the window), so the async diff fills the already-reserved area without moving the layout.
- [x] It would be nice to allow users to resize the commit editor input box if they prefer more editor and less diff displayed. This setting should then be remembered per repo — done: a drag divider between the message box and the diff preview resizes the message (60px minimum), persisted per worktree in `folds.toml` (`commit_editor_height`, omitted at the default).
- [x] It's not possible to dismiss the help transient on the commit editor in vim mode — done: `on_key` (which runs popups) bails while the editor is open, so the `?` menu was unreachable; `on_capture_key` now owns the keyboard when a Dispatch/Transient popup floats over the editor — Esc/C-g dismisses, other keys are swallowed instead of leaking into the message.
- [x] Commit editor's "discard message?" should say "discard changes?" when appropriate (eg rewording) — done: the prompt reads "Discard changes?" whenever the editor opened with a baseline message (amend/reword/release-tag seed).
- [x] There's lag after pressing ':' before the command palette opens, and there's lag when typing in it — done: (1) the picker overlay is now its own child view (`PickerOverlay`), so filtering/navigation repaints just the bottom panel instead of invalidating the whole window (title bar, status list, theme re-resolution) per keystroke; (2) `command_keys` resolves transient-leaf keys from a once-per-style table instead of constructing all fourteen transients per visible row; (3) the palette precomputes every row's key/id hints at open instead of lazily on first paint. (Also: much of any remaining lag is the debug build — text layout is 10-50x slower unoptimized; use `cargo run --release`.)
- [x] Commit editor feedback round: single divider line with a drag grip, diff loading spinner, non-blocking diff builds, in-box mode indicator, split commit-view loading, shorter vim setting description, and `:` ex commands — done: (1) removed the diff preview's own top border so the drag divider is the only line, and gave the divider a centered grip pill that brightens on hover; (2) the reserved diff area shows the app spinner + dim "loading diff" while the async load is in flight (`diff_loading`, distinct from "loaded empty"); (3) diff row building — including tree-sitter highlighting — moved onto the background executor (`highlight_diff` takes the theme instead of `&App`, spans are `Arc<[Span]>`, a `DiffStyle` snapshot crosses the thread), so typing stays live while a huge staged diff builds; (4) the commit detail view loads in two stages — metadata + message render immediately with a "Loading diff…" note, the flattened/highlighted diff swaps in when ready (cache hits still populate synchronously); (5) the Vim mode indicator moved from a bar under the message into an overlay inside the message box's bottom-right corner, pending keys / prompts to the left of the mode chip (clicks pass through); (6) the settings toggle description is now one line; (7) Vim mode gained a basic ex line: `:` opens a prompt (Visual prefills `'<,'>`), supporting `[range]s/pat/rep/[flags]` (`%`, `N,M` with `.`/`$`, `g`/`i` flags, `&`/`\1`-`\9` replacement refs, escaped delimiters), `:q`, `:q!` (discard without prompt), `:w`/`:wq`/`:x` (commit), and bare `:N` line jumps — semantics probed against Vim 9.2 (`.` does not repeat `:s`), +8 engine tests.
- [x] Vim mode feedback: visible invalid-command feedback, `:s` live match highlight, prompt history, prominent prompt display, opaque mode chip, no soft wrap, `:help` — done: (1) a Beep now rings a visual bell (the mode chip briefly tints red), and failed `:` commands additionally echo their error in red until the next key ("Pattern not found: zzz", "Not an editor command: foo", …); (2) while typing `[range]s/pat…` the overlay highlights the matches the substitution would touch (first per line, all once `g` is typed, scoped to the range); (3) `Up`/`Down` (or `C-p`/`C-n`) at the `/` and `:` prompts recall executed lines (50 each, consecutive repeats collapsed, the live line stashed and restored past the newest); (4) the live `/`//`:` prompt renders as a full-color chip instead of dim hint text; (5) the indicator chips blend their fills opaque over the box background, so overlaid message text can't bleed through; (6) the commit editor no longer soft-wraps — a long summary stays the single line it will be (horizontal scroll), and the body still hard-wraps as you type; (7) `:help`/`:h` opens a Vim-mode cheat-sheet transient (Esc dismisses). +4 engine tests.
- [x] Vim/editor round 3: opaque prompt chips, tighter `:help`, `C-v` Visual Block, `zz`/`zt`/`zb`, no reflow chip in vim header, clickable mode chip, flush diff, no input padding, bottom-edge resize, which-key, `[vim.keymap]` remapping — done: (1) the pending-keys text and echoed error each sit on an opaque editor-background chip; (2) the `:help` sheet dropped the Modes group and history row, tightened descriptions, and shows alternates (`:wq`, `,,`, `:q`, `,k`) as kbd chips; (3) `C-v` blockwise Visual (probe-arbitrated against Vim 9.2): char-column rectangles with `$`-to-eol blocks, d/x/y/c/r, I/A with typed-text replication at Esc (A pads short lines, I/c skip them), a block register kind whose p/P paste column-aligned with padding/count, D/C to-eol forms, and dot-repeat of block edits; (4) `z` prefix scrolling — `zz`/`zt`/`zb` (+ `z.`/`z<CR>`/`z-`) via a new Scroll action against the input's scroll offset; (5) the header hides the gq reflow chip in vim mode (it lives in `:help`); (6) clicking the mode chip opens `:help`; (7) the diff preview runs flush to the window bottom; (8) the input's vertical dead space is gone; (9) the separate divider row is gone — the box's bottom edge drags to resize, grip pill on the border; (10) which-key: pending states (operators, `g`/`Z`/`z`/`,`, objects, surround, user prefixes) pop a compact hint panel after 600ms, inert overlay above the indicator; (11) `[vim.keymap]` maps literal key sequences to commit/cancel/discard/reflow/help, additive over the built-ins (a colliding first key shadows that built-in), per-entry repo overlay merge, surfaced in which-key; the live vim-mode toggle threads the map too. +17 test fns (225 total).
- [x] Which-key surround rows + transient-matching bg; ✕ on transient popups; vim overlay clipped to the box — done: (1) operator-pending which-key now lists `s` per operator (Add/Delete/Change surround); (2) the which-key panel fill matches the bottom transient panel (palette.panel, blended opaque); (3) transient popups (incl. the `?` dispatch and `:help`) show a ✕ at the top-right that behaves like Esc (a Configure sub-transient pops back to its parent); (4) the vim overlay (block cursor, selections, match highlights) is clipped to the message box via a content mask, so an expanded/resized box can't paint the cursor outside it — which exposed that the overlay canvas's `size_full` laid out to zero bounds (harmless unmasked, fatal masked); it's `inset_0` now.
- [x] Consistent kbd sequence rendering in vim mode + one which-key delay — done: vim multi-key sequences now render one keycap per keystroke like the rest of the app (`Z Z`, `g q`, `i w`, `, k` — not `ZZ`), across the `:help` sheet, the which-key panel (user-map remainders included), and the editor header; a `·` separator token (rendered as a bare dot) keeps alternative bindings distinct within one label (`Z Z · :wq · , ,`); ex command lines (`:wq`, `:s/pat/rep/`) stay single caps since they're typed at a prompt. The transient layout's cell-width estimate now measures keycaps per step, and a wide command group no longer fans into more sub-columns than fit the window (it stays tall instead of clipping at the right edge). The vim which-key panel uses the app-wide `which_key_delay_ms` (default 1000ms) instead of its own hardcoded 600ms.
- [x] First editor line flush with the box top + font size setting — done: (1) the commit editor's Input uses row-height lines (18px at the default size, like every list row in the app) instead of the inherited ~20px line height, so the first line sits against the box's top edge (the remaining ~1px is the font's own ascent inset); (2) new `font_size` config (default 13, clamped 9-24) with a Font size dropdown on the settings screen — the root text size, all `uniform_list` row heights (status/log/diff/picker, plus the scroll/paging and drag-beyond-list math), the editor's line height, and the small chrome sizes (title-bar pills, todo-editor hint) all derive from it. `ROW_HEIGHT` the const became `StatusView::row_h()`. Verified live at 16px and via the settings dropdown round-trip (global write, repo overlay precedence, live reload rescale).
- [x] `font_size` default follows the system — done: unset `font_size` (the default) now resolves to the platform's standard UI text size (`NSFont.systemFontSize` on macOS, 13 elsewhere/fallback) instead of a hardcoded 13; the config field became optional (omitted from the file when unset), and the settings dropdown leads with "System Default (N px)".
- [x] Font size fixes: "System Default" wrote `font_size = 0`; component widgets ignored the setting — done: (1) saving an unset `font_size` now removes the key from the config file (`set_setting`'s omit keeps an existing key, so switching back to System Default wrote `unwrap_or_default()` = 0, which loaded as the 9px clamp floor); a lingering `0` from the buggy build reads as unset; regression test added. (2) gpui-component widgets (Select dropdowns, switches) size in rems via the component theme's `font_size` (which also sets the window rem size) — `apply_appearance` now scales it with the effective size (16/13 ratio, so nothing changes at the 13px default), so the settings controls track the setting.
- [x] Command palette hangs the app for seconds on open — done: profiled with `sample`; the hint precompute mapped each displayed row back through `raw_command_title`, which re-expands every user `[[command]]`'s `{placeholder}` title per palette row, and `{default-branch}`/`{default-remote}` resolution shells out to git (remote HEAD + mainline probes) — O(rows × user commands) synchronous subprocesses on the UI thread (~6.7s with one placeholder command). Two fixes: (1) the palette builds key/id hints in the same pass as the entries, from each command's raw title — no reverse lookup; (2) the default-branch resolution is memoized on StatusView (cleared each refresh), so any placeholder expansion (palette, `?` menu, transients) resolves it at most once per refresh. Open latency measured 6.7s → 0.15s (debug build, including the injection round-trip).
- [x] The app name should be styled as "Magritte" (with the capital M) in window names, bundles, etc. — done (audit): every shippable surface already is — in-app strings and menu labels ("About Magritte", "Quit Magritte"), the OS window-title fallback, `CFBundleDisplayName`/`CFBundleName` in the macOS bundle, and the Linux .desktop Name. The one lowercase surface left is the *unbundled* dev binary's process name (menu-bar app menu, Dock, ⌘-tab), which macOS derives from the executable basename: `NSProcessInfo setProcessName:` was implemented and live-verified ineffective on current macOS (a minimal Cocoa control app confirmed LaunchServices ignores it), so it was not kept. Running the `.app` bundle shows "Magritte" everywhere; CLI output (`magritte 0.7.0`, usage) deliberately keeps the binary-name convention.
- [x] Have repos fall off the recently used list after 30 days since the last use — done: recent-repos entries now carry a `last_used` unix timestamp (`RecentRepo`), pruned past 30 days by a shared `RecentRepos::load` that both the Dock-menu rebuild and startup path use (stale entries drop on read, not only on write). A legacy `paths = [...]` file migrates in place with a fresh clock and the old key is never written back. +3 state tests (migration, pruning, round-trip).
- [x] Codebase review + refactor (fourth full pass) — done: eight parallel area deep-reads (vim engine, vim app layer, editor/diff pipeline, status/jobs, core git, input/dispatch, chrome/config, docs/tests), every finding adversarially verified before acting — 62 confirmed, 1 refuted. Fixed all of them: 2 high (non-UTF-8 hunk staging corrupted blobs via lossy decoding — patches are byte-preserving now; `p u` pushed the local branch name instead of the upstream ref) plus the rebase todo editor silently deleting exec/label/merge instructions, ~20 mediums (lost commit messages → per-worktree message ring with alt-p restore; live theme switches leaving stale baked colors; per-repo themes clobbering other windows; palette unavailable/unscoped off the status screen; vim prompts destroyed by unhandled keys; selection-dropping async rebuilds; uncancellable staging jobs; fixup -C parsing; @{push} divergence; font-size scaling stragglers; and more), and the low tail (fidelity probes, leaks, focus, docs). Structural: engine.rs split into vim/engine/{ex,block,edit,hints}.rs (pure motion, zero test edits); row construction fully pure (StatusRows borrow-struct + conflicted_paths) with the missing section tests; act-at-point resolution extracted pure with 13 tests. ~80 new tests across the workspace. One false alarm run to ground: palette type-to-filter appearing dead under the debug harness is the pre-existing backgrounded-frame focus quirk (verified identical on a pre-review baseline build), not a regression.
