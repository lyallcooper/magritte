[tools]
# Pinned for reproducibility (the app depends on specific gpui / gpui-component
# git revisions); bump deliberately rather than tracking "latest".
rust = { version = "1.96.0", components = "rustfmt,clippy" }
